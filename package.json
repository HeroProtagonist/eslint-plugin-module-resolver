{
  "name": "eslint-plugin-module-resolver",
  "version": "0.12.0",
  "description": "Warn when using relative paths to modules aliased",
  "repository": "HeroProtagonist/eslint-plugin-module-resolver",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "files": [
    "dist"
  ],
  "author": "Jordan",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "babel lib --out-dir dist",
    "build:watch": "yarn build --watch",
    "clean": "rimraf *.tgz dist",
    "bump": "yarn test && yarn version && yarn clean && yarn build && npm publish"
  },
  "dependencies": {
    "find-babel-config": "^1.2.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "babel-core": "^6.26.3",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.16.0",
    "jest": "^24.7.1",
    "nsp": "^3.2.1",
    "prettier": "^1.17.0",
    "regenerator-runtime": "^0.13.2",
    "rimraf": "^2.6.3"
  },
  "license": "MIT"
}
